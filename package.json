{
  "name": "ta26_watermeter",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "postinstall": "npm install --prefix server && cd server && npx prisma generate && cd .. && npm install --prefix client && npm run build --prefix client",
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
